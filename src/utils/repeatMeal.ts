import { FoodMoment } from '../types';
import { getLocalDateKey } from './dateKey';
const isDemoMoment=(moment:FoodMoment)=>/^moment-\d{1,2}$/.test(moment.id);
export function getRepeatCandidates(moments:FoodMoment[],limit=3):FoodMoment[]{const seen=new Set<string>();return[...moments].filter(moment=>!isDemoMoment(moment)).sort((a,b)=>(b.createdAt||0)-(a.createdAt||0)).filter(moment=>{const key=`${moment.category}|${moment.title.trim().toLowerCase()}`;if(seen.has(key))return false;seen.add(key);return true;}).slice(0,limit);}
export function repeatMeal(moment:FoodMoment,now=new Date()):FoodMoment{const createdAt=now.getTime();return{id:`moment-${createdAt}-${Math.random().toString(36).slice(2,6)}`,title:moment.title,label:moment.label,category:moment.category,date:getLocalDateKey(now),time:now.toTimeString().slice(0,5),location:'',locationCategory:undefined as unknown as FoodMoment['locationCategory'],imageUrl:moment.imageUrl,rating:undefined,mood:undefined as unknown as FoodMoment['mood'],hungerLevel:undefined,fullnessLevel:undefined,eatingPace:undefined,distraction:undefined,energyAfter:undefined,coachFeedback:undefined,notes:undefined,tags:['Repeated'],isFavorite:false,nutrition:moment.nutrition,price:undefined,companions:undefined,createdAt};}
