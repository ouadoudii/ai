import React from 'react';
import { Home, Activity, Plus } from 'lucide-react';
import { ActiveTab } from '../types';
import { useLanguage } from '../i18n';
interface MobileBottomNavProps { activeTab: ActiveTab; setActiveTab: (tab: ActiveTab) => void; onCapture: () => void; favoriteCount: number; }
export const MobileBottomNav: React.FC<MobileBottomNavProps> = ({ activeTab, setActiveTab, onCapture }) => { const {t}=useLanguage(); return <nav className="fixed bottom-0 left-0 right-0 z-40 px-3 pb-[max(.65rem,env(safe-area-inset-bottom))]" aria-label={t('mainNav')}><div className="max-w-sm mx-auto grid grid-cols-3 items-end rounded-[30px] bg-[#FFFDF9]/97 p-2.5 shadow-[0_18px_50px_rgba(48,38,28,.18)] backdrop-blur-xl border border-[#E9E1D5]"><button type="button" onClick={()=>setActiveTab('today')} className={`min-h-14 rounded-[20px] flex flex-col items-center justify-center gap-1 ${activeTab==='today'?'text-[#526B48]':'text-[#827D74]'}`}><Home className="w-5 h-5"/><span className="text-[10px] font-black">{t('today')}</span></button><button type="button" onClick={onCapture} className="relative -mt-8 mx-auto w-17 h-17 rounded-full bg-[#526B48] text-white flex items-center justify-center shadow-[0_14px_30px_rgba(82,107,72,.32)] border-[5px] border-[#FFFDF9]" aria-label={t('add')}><Plus className="w-8 h-8"/></button><button type="button" onClick={()=>setActiveTab('type_analysis')} className={`min-h-14 rounded-[20px] flex flex-col items-center justify-center gap-1 ${activeTab==='type_analysis'?'text-[#526B48]':'text-[#827D74]'}`}><Activity className="w-5 h-5"/><span className="text-[10px] font-black">{t('patterns')}</span></button></div></nav>; };
