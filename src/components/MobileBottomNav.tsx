import React from 'react';
import { Home, Activity, Plus } from 'lucide-react';
import { ActiveTab } from '../types';
import { useLanguage } from '../i18n';
interface MobileBottomNavProps { activeTab: ActiveTab; setActiveTab: (tab: ActiveTab) => void; onCapture: () => void; favoriteCount: number; }
export const MobileBottomNav: React.FC<MobileBottomNavProps> = ({ activeTab, setActiveTab, onCapture }) => { const {t}=useLanguage(); return <nav className="fixed bottom-0 left-0 right-0 z-40 px-4 pb-[max(.7rem,env(safe-area-inset-bottom))]" aria-label={t('mainNav')}><div className="max-w-sm mx-auto grid grid-cols-3 items-center rounded-[28px] bg-[#252824]/96 p-2 shadow-[0_18px_48px_rgba(30,34,29,.28)] backdrop-blur-xl"><button type="button" onClick={()=>setActiveTab('today')} className={`min-h-14 rounded-[20px] flex flex-col items-center justify-center gap-1 ${activeTab==='today'?'bg-white text-[#252824]':'text-white/65'}`}><Home className="w-5 h-5"/><span className="text-[10px] font-bold">{t('today')}</span></button><button type="button" onClick={onCapture} className="relative -mt-6 mx-auto w-16 h-16 rounded-[23px] bg-[#E76F45] text-white flex items-center justify-center shadow-[0_12px_30px_rgba(231,111,69,.35)] border-4 border-[#F7F5F0]" aria-label={t('add')}><Plus className="w-7 h-7"/></button><button type="button" onClick={()=>setActiveTab('type_analysis')} className={`min-h-14 rounded-[20px] flex flex-col items-center justify-center gap-1 ${activeTab==='type_analysis'?'bg-white text-[#252824]':'text-white/65'}`}><Activity className="w-5 h-5"/><span className="text-[10px] font-bold">{t('patterns')}</span></button></div></nav>; };
