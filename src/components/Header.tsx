import React from 'react';
import { Activity, Home, Plus } from 'lucide-react';
import { ActiveTab, FilterState } from '../types';
interface HeaderProps { activeTab: ActiveTab; setActiveTab: (tab: ActiveTab) => void; onOpenAddModal: () => void; onOpenCheckInModal: () => void; filterState: FilterState; setFilterState: React.Dispatch<React.SetStateAction<FilterState>>; isMobilePreviewMode: boolean; setIsMobilePreviewMode: (mode: boolean) => void; totalMoments: number; }
export const Header: React.FC<HeaderProps> = ({ activeTab,setActiveTab,onOpenAddModal }) => <header className="sticky top-0 z-30 bg-[#F7F5F0]/92 backdrop-blur-xl border-b border-[#EAE5DC]/70"><div className="max-w-5xl mx-auto px-4 sm:px-6 h-16 flex items-center justify-between gap-4"><button onClick={()=>setActiveTab('today')} className="font-display font-black text-xl tracking-tight text-[#252824]">Rhythm</button><nav className="hidden md:flex items-center gap-1 bg-white rounded-full p-1 border border-[#E8E3DA]" aria-label="Main navigation"><button onClick={()=>setActiveTab('today')} className={`rounded-full px-4 py-2 text-xs font-bold flex gap-1.5 items-center ${activeTab==='today'?'bg-[#252824] text-white':'text-[#706F68]'}`}><Home className="w-3.5 h-3.5"/>Today</button><button onClick={onOpenAddModal} className="rounded-full px-4 py-2 text-xs font-bold flex gap-1.5 items-center text-[#9B4D32]"><Plus className="w-3.5 h-3.5"/>Add</button><button onClick={()=>setActiveTab('type_analysis')} className={`rounded-full px-4 py-2 text-xs font-bold flex gap-1.5 items-center ${activeTab==='type_analysis'?'bg-[#252824] text-white':'text-[#706F68]'}`}><Activity className="w-3.5 h-3.5"/>Patterns</button></nav><span className="text-[11px] font-bold text-[#8A867E]">Sleep · Food · Energy</span></div></header>;
